using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloooWorld!! : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(“Hellooo World!”);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
